{
  "name": "dbdog-agent-obs",
  "version": "0.6.0",
  "description": "dbdog Agent Observability client hooks for Claude Code（mint → propagate → synthesize → sweep；子代理内部调用可见，span 树三层。脚本本体在 claude-code-hooks/，hooks/hooks.json 经 ${CLAUDE_PLUGIN_ROOT} 引用；另带两个 skill：span-graph（hook span → 假设图 markdown,零模型）、evidence-chain（现象+根因+修复 diff+源码树 → 应有证据链与 dbdog 工具缺口 markdown），见 skills/*/SKILL.md）"
}
