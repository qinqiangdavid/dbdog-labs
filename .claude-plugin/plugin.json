{
  "name": "dbdog-agent-obs",
  "version": "0.5.0",
  "description": "dbdog Agent Observability client hooks for Claude Code（mint → propagate → synthesize → sweep；子代理内部调用可见，span 树三层。脚本本体在 claude-code-hooks/，hooks/hooks.json 经 ${CLAUDE_PLUGIN_ROOT} 引用；另带 skill diag-pair：正向（hook span → 假设图 markdown）/ 反向（根因+源码 → 应有证据链 markdown），见 skills/diag-pair/SKILL.md）"
}
